{
  "name": "elena-gofman-seo-site",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "node scripts/run_python.cjs scripts/build_site.py",
    "test": "node scripts/run_python.cjs -m unittest discover -s tests -v",
    "dev": "npm run build && wrangler dev",
    "deploy": "npm run build && wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "4.125.0"
  }
}
